# Test run outputs are ephemeral and can be regenerated by re-running the
# corresponding test_pXX_*.zsh entry-points.
results/

# Upstream plugin cache, fetched on first `make test-upstream`.
.upstream/
